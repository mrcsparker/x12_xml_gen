require "spec_helper"

RSpec.describe X12XmlGen do
  it "has a version number" do
    expect(X12XmlGen::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
